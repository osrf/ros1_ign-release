From a347681b65b0e81785d07da054a6045414c121b1 Mon Sep 17 00:00:00 2001
From: Jose Luis Rivero <jrivero@osrfoundation.org>
Date: Fri, 6 Sep 2019 19:26:01 +0200
Subject: [PATCH 1/4] Replace metadata for ros_ign

---
 debian/control.em | 13 ++++++++++++-
 1 file changed, 12 insertions(+), 1 deletion(-)

diff --git a/debian/control.em b/debian/control.em
index 7cbf65a..c290bb7 100644
--- a/debian/control.em
+++ b/debian/control.em
@@ -10,5 +10,16 @@ Package: @(Package)
 Architecture: any
 Depends: ${shlibs:Depends}, ${misc:Depends}, @(', '.join(Depends))
 @[if Conflicts]Conflicts: @(', '.join(Conflicts))@\n@[end if]@
-@[if Replaces]Replaces: @(', '.join(Replaces))@\n@[end if]@
+@[if Replaces]
+Replaces: @(', '.join(Replaces))@ (<< 0.7.0-1)\n
+Breaks: @(', '.join(Replaces))@ (<< 0.7.0-1)\n
+@[end if]@
 Description: @(Description)
+
+Package: @(Package.replace('ros_','ros1_'))
+Depends: @(Package), ${misc:Depends}
+Architecture: all
+Priority: optional
+Section: oldlibs
+Description: transitional package
+ This is a transitional package. It can safely be removed.
-- 
2.17.1

